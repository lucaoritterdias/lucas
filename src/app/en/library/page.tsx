import Link from "next/link";
import { Header } from "../../components/Header";
import { getBooks } from "../../library";
export const metadata = { title: "Library · Lucas Ritter Dias", description: "Books, ideas, and reading notes by Lucas Ritter Dias." };
export default function LibraryPage() { const books = getBooks(); return <><Header/><main className="route-page library-page"><header className="route-hero"><p>Personal library</p><h1>Books I read.<br/><em>Ideas that stayed.</em></h1><span>A collection of notes, lessons, and reflections after the final page.</span></header>{books.length ? <section className="book-grid">{books.map((book,index) => <Link className="book-card" href={`/en/library/${book.slug}`} key={book.slug}><div className="book-cover" style={book.cover ? {backgroundImage:`url(${book.cover})`} : undefined}><span>{String(index+1).padStart(2,"0")}</span><strong>{book.title}</strong><small>{book.author}</small></div><p>{book.readAt || "Finished"}</p><h2>{book.title}</h2><span>{book.excerpt}</span></Link>)}</section> : <section className="library-empty"><span>01</span><div><h2>The shelf is ready.</h2><p>Add the first Markdown file to <code>src/content/library</code>.</p></div></section>}</main></>; }
